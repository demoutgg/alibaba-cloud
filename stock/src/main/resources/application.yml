server:
  port: 8020
#数据源配置
spring:
  application:
    name: stock-service
  datasource:
    username: zhong
    data-password: zjr0418!
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://rm-bp100kx6azg1h7z2f6o.mysql.rds.aliyuncs.com:3306/springboot?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    #   指定数据源的类型
    type: com.alibaba.druid.pool.DruidDataSource
    #    schema:
    #        - classpath:employee.sql
    # 数据源其他配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    #filters: stat,wall
    #maxPoolPreparedStatementPerConnectionSize: 20
    #useGlobalDataSourceStat: true
    #connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500


#mybatis 配置
mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.demou.study.entity

#showSql
logging:
  level:
    com:
      example:
        mapper: debug


